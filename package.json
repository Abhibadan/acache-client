{
  "name": "dummy-redis-client",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/index.js",
    "cli": "node ./dist/cli.js",
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "build": "tsc"
  },
  "bin": {
    "acache-cli": "./dist/cli.js"
  },
  "keywords": [],
  "author": "Abhibadan Ghosh",
  "license": "ISC",
  "description": "This is the client package, where cli and npm package is available for client access",
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "undici-types": "^6.20.0"
  }
}
